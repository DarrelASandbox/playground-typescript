"use strict";
// No issue
// function getRandomElement<T>(arr: T[]): T {
//   return arr[Math.floor(Math.random() * arr.length)];
// }
// Add trailing comma for `.tsx` file using arrow function
var getRandomElement2 = function (arr) {
    return arr[Math.floor(Math.random() * arr.length)];
};
